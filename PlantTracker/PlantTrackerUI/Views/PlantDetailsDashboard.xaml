<UserControl x:Class="PlantTrackerUI.Views.PlantDetailsDashboard"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:PlantTrackerUI.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" MinWidth="300">
    <Border BorderThickness="1" BorderBrush="#d9cb98">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="15"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <TextBlock Grid.Row="0" Text="Name:" />
            <TextBlock Grid.Row="0" Grid.Column="2" Text="{Binding SelectedPlant.Name}"/>
            <TextBlock Grid.Row="1" Text="Type(s):" />
            <WrapPanel Grid.Row="1" Grid.Column="2">
                <ItemsControl x:Name="PlantTypeItemsControl" ItemsSource="{Binding SelectedPlant.PlantTypes}">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <WrapPanel />
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border BorderThickness="1" BorderBrush="#d9cb98" Background="#f7eecd" Margin="0 0 5 0">
                                <StackPanel x:Name="ItemsStackPanel" Orientation="Horizontal">
                                    <TextBlock Text="{Binding Name}" Margin="5 2"/>
                                    <Button BorderThickness="0" Margin="0 1 3 0" Style="{StaticResource XButton}"
                                            Command="{Binding ElementName=PlantTypeItemsControl, Path=DataContext.RemoveSelectedTypeCommand}"
                                            CommandParameter="{Binding}"/>
                                    

                                </StackPanel>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
                <Border BorderThickness="1" BorderBrush="#8adb96" Background="#cdf7d3" Margin="0 0 5 0">
                    <StackPanel Orientation="Horizontal" >
                        <Button Background="#cdf7d3" BorderThickness="0" Padding="5 0"
                                Command="{Binding OpenAddPlantTypeWindowCommand}">
                            AddType...
                        </Button>
                    </StackPanel>
                </Border>
            </WrapPanel>
            <TextBlock Grid.Row="2" Text="Planting date" />
            <TextBlock Grid.Row="2" Grid.Column="2" Text="{Binding SelectedPlant.PlantingDateString}"/>

            <!-- <TextBlock Grid.Row="3" Text="Last watering date" /> -->
            <!-- <TextBlock Grid.Row="4" Text="Container(s):" /> -->
            <!-- <TextBlock Grid.Row="5" Text="Watering system(s):" />-->
            <!-- <TextBlock Grid.Row="6" Text="Position:" /> -->
        </Grid>
    </Border>
</UserControl>
